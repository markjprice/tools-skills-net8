Chapter 13 677 3. In Suppliers.cshtml, replace the existing contents with markup to render a heading and an HTML table containing the supplier company names, as shown in the following markup: @page @model Northwind.Web.Pages.SuppliersModel <div class="row"> <h1 class="display-2">Suppliers</h1> <table class="table"> <thead class="thead-inverse"> <tr> <th>Company Name</th> </tr> </thead> <tbody> @if (Model.Suppliers is not null) { @foreach(string name in Model.Suppliers) { <tr> <td>@name</td> </tr> } } </tbody> </table> </div> While reviewing the preceding markup, note the following: • The model type for this Razor Page is set to SuppliersModel. This is the type for the Model, which we can access anywhere within the HTML markup. property named • The page outputs an HTML table with Bootstrap styles. @if and @for statements to embed C# code in HTML. • The page uses Razor syntax Suppliers property • The data rows in the table are generated by looping through the of Model if it is not null. 4. Start the website using the https launch profile and visit it using Chrome. 